@using NavOS.Basecode.Services.ServiceModels
@model AdminViewModel

@{
    ViewData["Title"] = "Reset Password";
    ViewBag.LoginView = true;
    ViewBag.MainStyle = true;
}

@section styles {
    <link rel="stylesheet" type="text/css" href="~/css/login-verify.css">
}

<div class="centerBox">
    <div class="box">
        <header>
            <h1>Reset Password</h1>
            <h5>Enter your new password</h5>
        </header>
        <div>
            <form asp-action="ResetPassword" method="post">
                <input type="hidden" asp-for="AdminId" value="@ViewBag.AdminId" />
                <input type="hidden" asp-for="Token" value="@ViewBag.Token" />
                <div class="w3-container" style="padding-top:15px;">
                    <div class="txt_field" style="margin: 35px 35px 0px 35px;">
                        <i class="fa fa-unlock-alt"></i>
                        <input type="password" asp-for="Password" placeholder="New Password" onkeypress="return event.charCode != 32">
                    </div>
                        <span asp-validation-for="Password" class="text-danger" style="height:20px; margin-left:35px;"></span>
                    <div class="txt_field" style="margin: 20px 35px 0px 35px;">
                        <i class="fa fa-unlock-alt"></i>
                        <input type="password" asp-for="ConfirmPassword" placeholder="Confirm Password" onkeypress="return event.charCode != 32">
                    </div>
                        <span asp-validation-for="ConfirmPassword" class="text-danger" style="height:20px; margin-left:35px;"></span>
                    <div class="w3-full w3-center" style="margin-bottom: 35px;margin-top: 20px">
                        <input type="submit" name="submit" value="Submit">
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>

@section scripts {
    <script>
        var errorMessage = '@TempData["ErrorMessage"]';
        var successMessage = '@TempData["SuccessMessage"]';

        if (errorMessage) {
            toastr.error(errorMessage);
        }

        if (successMessage) {
            toastr.success(successMessage);
        }
    </script>
}